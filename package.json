{
  "name": "halal-info-generator",
  "version": "1.0.0",
  "description": "Generates HTML pages from CSV data",
  "main": "generate.js",
  "scripts": {
    "generate": "node generate.js",
    "build": "npm run generate"
  },
  "dependencies": {
    "csv-parser": "^3.0.0"
  }
}